# CGV-style Cinema Frontend

- Next.js 14, React 18, TypeScript strict
- Zustand, React Query
- ESLint, Prettier
- API: Connects to backend via REST endpoints
- No direct DB access
- No `any`, only `type` for types
- End-to-end flow: Movie → Showtime → Seat → Hold → Payment → Ticket
